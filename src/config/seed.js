// src/config/seed.js — DISABLED FOR PRODUCTION
// This file previously contained demo seed data.
// It has been intentionally cleared. The system starts fresh.
// All data must be entered through the application UI.

console.log('ℹ️  Demo seed disabled. System starts fresh.');
process.exit(0);
