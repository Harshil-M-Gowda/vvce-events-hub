// migrations/seed.js
// ⚠️  This is intentionally EMPTY for production.
// The system starts fresh with zero data.
// Use this file only for local dev if needed.

require('dotenv').config();
console.log('ℹ️  No seed data — system starts fresh. Add data through the application UI.');
process.exit(0);
